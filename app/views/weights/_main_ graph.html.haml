.Main-graph
  .Top-items
    .Top-items__title
      = current_user.name
      の体重グラフ
    .Top-items__icon
    = link_to edit_user_path(current_user) do
      = icon('fas', 'walking', class: "Top-items__icon__btn-icon" )

  .Weight
  = line_chart @data.group("date(created_at)").sum(:kg), xtitle: "日付", ytitle: "kg"
