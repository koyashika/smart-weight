.MainGraph
  .TopItems
    .TopItems__title
      = current_user.name
      の体重グラフ
    .TopItems__icon
    = link_to edit_user_path(current_user) do
      = icon('fas', 'walking', class: "TopItems__icon__btn-icon" )

  .Weight
    = line_chart @weights.group("date(created_at)").sum(:kg), xtitle: "日付", ytitle: "kg", curve: false, width: "750px", height: "80vh"
